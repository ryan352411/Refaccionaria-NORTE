<Window x:Class="RefaccionariaPOS.Views.VentaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Punto de Venta - Mostrador"
        Height="760"
        Width="1180"
        MinHeight="680"
        MinWidth="1050"
        WindowStartupLocation="CenterScreen" Background="#F1F2F6">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" Margin="15" CornerRadius="10" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Carrito de compras actual" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Los productos agregados se muestran aqui." FontSize="12" Foreground="#7F8C8D"/>
                </StackPanel>

                <DataGrid x:Name="dgCarrito" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="White" RowHeight="35" RowHeaderWidth="0" AlternatingRowBackground="#F8F9FA"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Codigo" Binding="{Binding CodigoBarras}" Width="130" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Refaccion / Producto" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Precio U." Binding="{Binding PrecioVenta, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="60"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="100" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>

                <DockPanel Grid.Row="2" Margin="0,15,0,0">
                    <Button x:Name="btnCobrar" Content="Registrar venta" DockPanel.Dock="Right" Width="190" Height="46"
                            Background="#2ECC71" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnCobrar_Click"/>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0" Orientation="Horizontal">
                        <TextBlock Text="TOTAL: " FontSize="22" FontWeight="Bold" Foreground="#34495E"/>
                        <TextBlock x:Name="lblTotalCarrito" Text="$0.00" FontSize="26" FontWeight="Black" Foreground="#E74C3C"/>
                    </StackPanel>
                </DockPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#ECF0F1" Margin="0,15,15,15" CornerRadius="10" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Busqueda de refacciones" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                    <TextBlock Text="Escribe codigo o nombre:" FontSize="12" Foreground="#7F8C8D"/>

                    <TextBox x:Name="txtBuscarId" Height="35" FontSize="14" Padding="5,7,5,5" TextChanged="TxtBuscarId_TextChanged" KeyDown="TxtBuscarId_KeyDown"/>
                    <TextBlock Text="Escaner USB activo: escanea el codigo y se agregara con Enter." FontSize="11" Foreground="#2563EB" Margin="0,8,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgResultadosBusqueda" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="DgResultadosBusqueda_SelectionChanged"
                          Background="White" RowHeight="35" HeadersVisibility="Column" Visibility="Collapsed" Cursor="Hand">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Codigo" Binding="{Binding CodigoBarras}" Width="80"/>
                        <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding PrecioVenta, StringFormat=C}" Width="70" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="1" x:Name="brdPreview" Background="White" CornerRadius="5" Padding="15" Visibility="Collapsed" BorderBrush="#BDC3C7" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Vista previa del articulo" FontSize="14" FontWeight="Bold" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                        <TextBlock Text="Codigo de barras:" FontSize="11" Foreground="#95A5A6"/>
                        <TextBlock x:Name="lblPreviewCodigo" Text="---" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <TextBlock Text="Nombre / descripcion:" FontSize="11" Foreground="#95A5A6"/>
                        <TextBlock x:Name="lblPreviewNombre" Text="---" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <TextBlock Text="Precio de Venta:" FontSize="11" Foreground="#95A5A6"/>
                        <TextBlock x:Name="lblPreviewPrecio" Text="$0.00" FontSize="20" FontWeight="Black" Foreground="#2980B9" Margin="0,0,0,15"/>

                        <TextBlock Text="Existencias Actuales (Stock):" FontSize="11" Foreground="#95A5A6"/>
                        <TextBlock x:Name="lblPreviewStock" Text="---" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <Separator Background="#BDC3C7" Margin="0,5,0,15"/>

                        <TextBlock Text="Cantidad a Agregar:" FontSize="11" Foreground="#95A5A6"/>
                        <TextBox x:Name="txtCantidadAgregar" Text="1" Height="35" FontSize="16" FontWeight="Bold" TextAlignment="Center" Padding="5,7,5,5"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <Border Background="White" CornerRadius="8" Padding="12" BorderBrush="#BBF7D0" BorderThickness="1" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Cobro" FontSize="13" FontWeight="Bold" Foreground="#14532D"/>
                            <TextBlock Text="Cliente frecuente" FontSize="11" Foreground="#64748B" Margin="0,8,0,3"/>
                            <ComboBox x:Name="cmbClientes" DisplayMemberPath="Nombre" SelectedValuePath="Id"/>
                            <TextBlock Text="Metodo de pago" FontSize="11" Foreground="#64748B" Margin="0,8,0,3"/>
                            <ComboBox x:Name="cmbMetodoPago" SelectedIndex="0" SelectionChanged="CmbMetodoPago_SelectionChanged">
                                <ComboBoxItem Content="Efectivo"/>
                                <ComboBoxItem Content="Tarjeta"/>
                                <ComboBoxItem Content="Transferencia"/>
                            </ComboBox>
                            <TextBlock Text="Efectivo recibido" FontSize="11" Foreground="#64748B" Margin="0,8,0,3"/>
                            <TextBox x:Name="txtEfectivoRecibido" Height="32" TextChanged="TxtEfectivoRecibido_TextChanged"/>
                            <DockPanel Margin="0,8,0,0">
                                <TextBlock Text="Cambio" FontWeight="Bold" Foreground="#334155"/>
                                <TextBlock x:Name="lblCambio" Text="$0.00" DockPanel.Dock="Right" FontWeight="Black" Foreground="#16A34A" HorizontalAlignment="Right"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="8" Padding="12" BorderBrush="#BFDBFE" BorderThickness="1" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Impresora de Tickets" FontSize="13" FontWeight="Bold" Foreground="#1E3A8A"/>
                            <ComboBox x:Name="cmbImpresoras" Margin="0,8,0,8"/>
                            <DockPanel>
                                <CheckBox x:Name="chkImprimirTicket" Content="Imprimir al cobrar" IsChecked="True" VerticalAlignment="Center"/>
                                <Button x:Name="btnProbarImpresora" Content="Probar" DockPanel.Dock="Right" Width="88" Background="#06B6D4" Click="BtnProbarImpresora_Click"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>

                    <Button x:Name="btnAgregarAlCarrito" Content="Agregar al carrito" Height="46"
                            Background="#E67E22" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnAgregarAlCarrito_Click" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
